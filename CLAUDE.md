# CLAUDE.md

See [AGENTS.md](AGENTS.md) — the shared agent guide (Codex + Claude Code) for this repo.
All project rules, layout, server workflow, and current state live there.
`docs/pipeline_v2.md` is the source of truth for the v2 pipeline (architecture, gates, results);
`docs/method.md` is the MPM equation contract.
