Utility Scripts
===============

This directory collects lower-priority helper scripts that used to live in the
repository root (batch runners, one-off analysis/regeneration utilities, etc.).

None of these are required for day-to-day training. Run them explicitly via:

```
python tools/<script>.py [...]
```

Keeping them here keeps the root clean while still preserving the functionality
should it be needed later.
