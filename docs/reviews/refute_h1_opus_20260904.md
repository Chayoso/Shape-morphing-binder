# REFUTE review — H⁻¹ mass-balance tranche (Opus subagent, 2026-09-04)

Reviewed the same tranche as the Codex round (docs/reviews/refute_h1_codex_20260904.md) plus
the uncommitted Codex fixes. Reviewer verified: FFT/Parseval exact (≤2.2e-16 vs full fftn),
autograd vs finite differences 1.2e-7, pad-2 vs pad-6 gradient change 2e-5, DC drop benign
(Σr = 3.6e-12; out-of-range CIC masked), the "L2 weak inside the band" diagnosis reproduced
(|g_vol| 27× weaker in the band core than at the poles). Verdict: do not adopt before the
geometry is measured at equal d_vol. Implementer responses:

| # | Finding | Response | Status |
|---|---|---|---|
| 1 | Most severe: the linear H⁻¹ residual carries a density-independent CIC self-term — a lattice attractor at the cell centre, one per loss cell (self-force +0.078 constant from 1 to 54 ppc; 44× D_vol's at mid-run), at the sub-cell clustering scale that killed the KDE family | Real and important. Fixed by the P3M self-energy subtraction in `d_h1` (analytic ½m²Σw_aw_bG(δ), Green's matrix from the same kernel): the self-force is now exactly zero at every sub-cell position (test). Measured on the v3 state before/after (band_push). The x3 signature (d_vol better, out_nn/cluster worse) is the pre-registered failure for v7/v8; the paired-at-equal-d_vol census (`paired_census.py`: cluster ratio + CIC sub-cell fraction histogram) is the decisive test. | FIXED; census done: cluster ratio 1.23→1.18, CIC centre excess +0.6%→+1.9% (faint), no x3 signature — see experiments.md |
| 2 | Source calibration drifts ×5.3 along a morph; scale uncapped (v5 lesson not carried) | Cap added (min(·, 1e3)). Drift is now MEASURED every window (`h1_ratio` = s·|g_h1|/|g_vol| in the window stats and history) rather than assumed; a per-window balancer is the follow-up if the ratio collapses below ~0.2 before the descent is done. One-shot stays the pre-registered rule. | CAP FIXED; drift logged |
| 3 | The Codex-F3 move out of `phys_core` left H⁻¹ as the one large unprojected, unbalanced gradient (cos(g_vol,g_h1) 0.03 → −0.12; 37–43% of particles in opposite half-spaces); the W1 precedent does not transfer (W1 is bounded) | Agreed: control before attribution. H⁻¹ is back inside `phys_core` (λ-balanced, PCGrad reference). Attribution (Codex F3) is handled by the v7c isolation arm and the paired census, not by weakening the control. | REVERTED to in-core |
| 4 | Gate: `last_reject_score` not reset across a null commit; scalar-merit collisions | Reset added in the null-commit branch (streak and signature). Collisions within 1e-4 relative between consecutive brake-rejected distinct candidates cost at most one stale count; accepted. Offline gate replay on r4_record40/b4 queued with the v7 verdict. | FIXED |
| 5 | The nonlocality test was a symmetry tautology (probe at frac 0.5) | Rewritten: probe swept over six sub-cell offsets, self-corrected H⁻¹ must point +x at every offset; a separate test shows D_vol's off-centre gradient is its own self-force (points −x, away from the deficit). Plus a direct self-force-zero test. | FIXED |
| 6 | band_push reported the outward-normal on the wrong stratum (off-support); on the on-support surface shell the synthetic H⁻¹ outward component is +0.79 with 100% pushed outward | Probe rewritten with the on-support surface shell (loss cells within one cell of an empty target cell, outward normal from the blurred occupancy). Numbers recorded in root_analysis.md; the arbiter remains v7/v8 out_nn, far and per-commit floater_frac. | MEASURED |
| 7 | v4/v6 falsify schedules, not signals; identical chamfer 0.1619 suspicious | Wording corrected in experiments.md. Chamfers are 0.16195 vs 0.16194 for different states (frames 1022 vs 1305, d_vol 46 vs 43) at the 20k volumetric chamfer floor — coincidence, not transcription. v4's untried reject-type split is noted; the line stays closed on the amplitude evidence. | DOCS FIXED |
| 8 | Addendum 7 argues visibility in Lagrangian units; pooled stats; no max | Amplitude metric added (drift-removed p2p, ear stratum), max reported (1.03 sp, one particle), pixel conversion added (~1.8 px per sp: p99 0.8 px). Rendered-flow measurement not done; the stratum-level p99 is the proxy. | DOCS STRENGTHENED |
| g | What could fake v7a: `improved` OR-clause, merit dilution, d_vol blind to sub-cell quality | Paired-at-equal-d_vol census implemented (`paired_census.py`); offline gate replay with h1 clauses stripped queued for the v7a archive; census prints delivered vs min-d_vol commit. Process note accepted: v7a/v7b = commit 7ed1337 code (in-core, no self-correction, c2f recalibration live), v7c = 6a654df (out-of-core); v8 = self-corrected in-core. | MEASUREMENTS QUEUED |

Agreed decisive measurement: v7a at its first commit with d_vol ≤ 30 vs v1 at its best
(a134, d_vol 29.95): chamfer, out_nn, far, uncovered, cluster ratio, CIC sub-cell histogram.
